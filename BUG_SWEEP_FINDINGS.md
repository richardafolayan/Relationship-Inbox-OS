# v1 Bug Sweep — Findings Log (2026-06-05/06)

Dynamic-workflow (Ultracode) bug sweep over all 206 source files on `v1/strip-back-pr1`.
Pipeline: fan-out readers → adversarial refute-by-default verification → fix-design → adversarial review → apply → test → PR → squash-merge.

## Summary

- **42 raw findings** → after adversarial verification: **7 High + 19 Medium confirmed** (4 Medium-tier refuted), 12 Low logged.
- **Fixed, tested, and merged to v1:** all 7 Highs, both Mediums re-rated High (M15, M19), 11 Mediums, a 4-fix low-risk hardening batch, plus **M2** (#548) and **M18** (#546) — each with a regression test. Full suite green.
- **M2** (`scan-queue.ts`, Low — preserve `sentVia=automation`/`replyToMessageId` when collapsing outbound twins) and **M18** (`repair-linkedin-synthetic-timestamps.ts`, Medium — only anchor the genuinely-last synthetic cluster to `lastMessageAt`) were initially deferred because a concurrent session held uncommitted edits to those exact files in the shared working tree; they were subsequently implemented + tested in an isolated git worktree (off clean v1) and merged, without disturbing that session's uncommitted work.

## Refuted Medium findings (verified NOT real — no issue filed)

- M8 imessage-db immutable=1 (readonly sufficient in practice)
- M9 send.ts enqueueSend PENDING race (P2002 fallback only on genuine duplicate)
- M11 send.ts outbound upsert overwrite (collision not reachable)
- M13 repair-linkedin-threads needsReply (recompute already respects archived/snoozed)

## Low-severity findings (logged only — not individually filed, per review policy)

These are real but low-impact. Listed for future attention; none filed as issues.

### L1. composerSource not reset after send/schedule leaves the AI-predraft badge over an empty composer
- **File:** `apps/dashboard/app/thread/[id]/page.tsx:1070`
- **Category:** Incorrect UI state
- **Detail:** onSend (line 1070) and scheduleSend (line 1325) both clear the composer text via setComposer("") but never reset composerSource. composerSource only resets to "empty" on a threadId change (line 886), on Discard (line 3595), and on Delete-draft (line 2703). When the operator sends or schedules an AI predraft (composerSource === "predraft") while staying on the same thread, the textarea becomes empty but composerSource stays "predraft". The render then shows the accent border + soft ring (line 3554) and the "AI predraft · review before sending" badge with the Discard button (line 3578) framing a now-empty input until the operator types (which flips source to "user" via the onChange at line 3617) or navigates away.
- **Suggested fix:** In onSend's optimistic-clear block (after setComposer("") at line 1070) and in scheduleSend's success block (after setComposer("") at line 1325), also call setComposerSource("empty").

### L2. Failed send with attachments clobbers newly-staged attachments and leaks their object URLs
- **File:** `apps/dashboard/app/thread/[id]/page.tsx:1118`
- **Category:** Data loss / resource leak (race)
- **Detail:** onSend snapshots attachmentsToSend (line 1067) and clears composerAttachments to [] (line 1071) before awaiting the network call. On failure the catch restores state with setComposerAttachments(attachmentsToSend) (line 1118), unconditionally overwriting whatever is currently in state. If the operator stages new attachments while the send is in flight (addFiles appends to the now-empty list), those new entries are discarded by the restore, and because removeAttachment/onSend never revoke their previewUrl, the createObjectURL handles created in addFiles (line 1132) leak.
- **Suggested fix:** In the catch, merge rather than overwrite (e.g. setComposerAttachments(prev => [...attachmentsToSend, ...prev])) or, before restoring, revoke the previewUrls of any current attachments being dropped. Simplest correct fix: prepend the failed attachments to the current state instead of replacing it.

### L3. enqueueScan always returns status "queued", never "running"
- **File:** `apps/runner/src/services/scan-queue.ts:949`
- **Category:** logic error (incorrect status reporting)
- **Detail:** enqueueScan calls `triggerProcessNext()` at line 944, then at line 949 returns `status: processing ? "queued" : "running"`. `triggerProcessNext()` invokes `void processNext()`, and processNext runs synchronously up to its first await: it checks `if (processing) return`, shifts the job, and sets `processing = true` before `await runJob(next)` (whose first await is inside runJob/ensurePlatformRows). Therefore by the time control returns to line 949, `processing` is already `true`, so the ternary always evaluates to "queued" — even for the very job that has just started running. The "running" branch is effectively dead.
- **Suggested fix:** Capture the running/queued decision BEFORE calling triggerProcessNext (e.g. `const willRunImmediately = !processing;` computed before `queue.push`/`triggerProcessNext`), and return `status: willRunImmediately ? "running" : "queued"`.

### L4. IMessageDb constructor leaks the open SQLite handle when the pragma or smoke-test throws
- **File:** `apps/runner/src/platforms/imessage-db.ts:248`
- **Category:** resource leak
- **Detail:** The constructor opens the database at line 248 (`this.db = new Database(...)`), then runs `pragma('journal_mode = WAL')` (line 249) and a smoke-test `SELECT 1 FROM chat LIMIT 1` (line 251). If either of those throws — e.g. chat.db is locked/SQLITE_BUSY while Messages.app is writing, the file is corrupt, Full Disk Access is partially revoked, or the `chat` table can't be read — the constructor propagates the error with the underlying SQLite connection still open. The only place that closes the handle is the instance method `close()`, but the half-constructed instance is never returned to the caller. In `imessage-adapter.ts` getDb() (lines 83-100) the `new IMessageDb(...)` is wrapped in try/catch that rethrows an AdapterFailure with no reference to close the dangling handle. Because getDb() is re-invoked on every scan poll of the always-on scan loop, each failed open leaks one SQLite connection / file descriptor, accumulating over time.
- **Suggested fix:** Wrap lines 249-251 in a try/catch that calls `this.db.close()` before rethrowing, e.g. `try { this.db.pragma('journal_mode = WAL'); this.db.prepare('SELECT 1 FROM chat LIMIT 1').get(); } catch (e) { try { this.db.close(); } catch {} throw e; }`.

### L5. uploadScreenshotToRepo cannot overwrite an existing path (no sha), so re-attaching the same reportId silently produces no comment
- **File:** `apps/runner/src/services/github-attachments.ts:133`
- **Category:** error-handling
- **Detail:** uploadScreenshotToRepo PUTs to repos/{repo}/contents/{path} without the existing file's `sha` (lines 133-144). GitHub's Contents API rejects a PUT to an already-existing path with HTTP 422 unless `sha` is supplied. attachScreenshotsToIssue derives a deterministic path `pilot-feedback-attachments/<reportId>-<n>.<ext>` (line 205), so a second attach run for the SAME reportId (duplicate Apps Script webhook, or a retry after the first run's comment-post failed) hits the existing files: every PUT returns !res.ok -> null, uploadedUrls stays empty, and the function returns { ok:false, reason:'all screenshot uploads failed' } at line 218 WITHOUT posting any comment — even though the images already exist in the repo. The operator's re-attempt appears to fail with no recovery.
- **Suggested fix:** Before PUT, GET the contents path; if it exists, include its `sha` to update in place (or treat 'already exists' as success and reuse the raw URL). At minimum, when an upload fails with 422-exists, still construct the raw URL and proceed to post the comment.

### L6. findIssueByReportId retry loop aborts on the first network error instead of retrying
- **File:** `apps/runner/src/services/github-attachments.ts:99`
- **Category:** error-handling
- **Detail:** The retry loop (lines 99-115) awaits fetchImpl() with no try/catch around the call. The retry/backoff (lines 112-114) only re-runs when res.ok is false or no item matched — a thrown error from fetchImpl (DNS failure, connection reset, GitHub 5xx that rejects) propagates straight out of findIssueByReportId, so the `attempts` retries are skipped entirely. The whole attach then fails on a single transient blip even though the issue may exist and a retry would have found it.
- **Suggested fix:** Wrap the fetch in try/catch inside the loop; on a thrown error, fall through to the delay+retry the same way a non-ok response does.

### L7. convertOnce has a non-atomic cache write: concurrent conversions of the same uncached source race on the same dst path
- **File:** `apps/runner/src/services/imessage-attachment-server.ts:127`
- **Category:** race condition
- **Detail:** `convertOnce` computes a cache path `dst`, checks `existsSync(dst)`, and if absent runs `sips`/`afconvert` writing directly to `dst`. Two concurrent requests for the same not-yet-cached HEIC/CAF/video both see `existsSync(dst) === false` and both spawn the converter writing to the identical `dst`. While request A is mid-write, request B's later `existsSync(dst)` (line 130) can observe a partially written file and return it, so `pipeFile` streams a truncated/garbled attachment to the dashboard. There is no temp-file + atomic rename and no in-flight lock.
- **Suggested fix:** Convert to a unique temp file (e.g. `${dst}.${pid}.${rand}.tmp`) then `renameSync` to `dst` (atomic on the same filesystem), or serialize conversions per `dst` with an in-process promise map keyed on `dst`.

### L8. resetPlatformInboxGraph reports inaccurate per-table delete counts because the four deleteMany calls race in Promise.all against the thread cascade
- **File:** `apps/runner/src/services/admin-reset.ts:110`
- **Category:** logic-error
- **Detail:** The sendRequest/draft/message/thread deleteMany calls are issued together inside a single Promise.all (lines 110-137). thread.deleteMany has onDelete: Cascade on Message, Draft and SendRequest (schema.prisma lines 386, 527, 587), and with SQLite foreign_keys enabled the parent thread delete cascades to its children natively. Since the four promises are created together and run over one connection without a defined ordering guarantee relative to the cascade, the message/draft/sendRequest deleteMany can execute after the thread rows (and thus their children) are already gone, returning a count of 0. The returned AdminResetDeleteCounts (lines 165-169) then under-reports how many rows were actually removed.
- **Suggested fix:** Run the child deletes (sendRequest, draft, message) before the thread delete in sequence (await each, or Promise.all only the children, then await thread.deleteMany), so each count reflects rows that existed at the time of its own delete. Alternatively scope the child deletes by the already-collected threadIds and run them strictly before thread.deleteMany.

### L9. tryAcquire has a TOCTOU race: a second concurrent caller queues and blocks instead of getting acquired:false
- **File:** `apps/runner/src/services/keyed-mutex.ts:78`
- **Category:** race condition
- **Detail:** tryAcquire reads `this.states.get(key)?.running` synchronously (line 80) and, when not running, calls `await this.runExclusive(key, work)` (line 83). runExclusive always ENQUEUES. If two callers both pass the `state?.running` check while the lock is free (the key state is absent or running===false at the moment each checks), the first one starts the holder and the second's runExclusive pushes its entry onto the queue and waits behind the first, then returns `{ acquired: true }`. The documented contract is that it 'Resolves with { acquired: false } synchronously when the lock is held'. In enrichment-queue, runOnce() (HTTP-triggered, no drainPass re-entrancy guard) and drainPass() can both call tryAcquire(enrichLockKey, () => visitProfile(...)) concurrently, so the same managed LinkedIn page can end up visited twice in sequence instead of one caller deferring — exactly the collision the lock was added to avoid (see enrichment-queue.ts:314 and :399 comments 'so two drains don't fight' / 'defer if the lock is held').
- **Suggested fix:** Make tryAcquire atomic: set/check a per-key `running` (or a dedicated held flag) synchronously before returning, e.g. inspect state and, if free, mark it acquired in the same synchronous tick before awaiting work — or short-circuit in enqueue with a mode that returns a 'not acquired' sentinel when state.running is already true at enqueue time, rather than re-reading state in tryAcquire and then calling the always-enqueuing runExclusive.

### L10. A single failed appendFile permanently poisons the writeQueue chain, breaking all subsequent log writes in the smoke run
- **File:** `apps/runner/src/services/linkedin-smoke-logger.ts:110`
- **Category:** error handling
- **Detail:** emit() chains writes via `writeQueue = writeQueue.then(() => appendFile(prettyLogPath, ...))` with no `.catch`. If one appendFile rejects (disk full, file removed, permission change mid-run), writeQueue becomes a rejected promise. Every subsequent emit() does `writeQueue.then(...)` (which carries the rejection forward) and then `await writeQueue`, so each later logLine/logStep rejects. Because the smoke endpoint awaits these (index.ts:5486-5505 `await smokeLogger.logLine(...)`), the first transient write failure aborts the remaining logging/steps of the smoke run rather than degrading to best-effort.
- **Suggested fix:** Isolate each write: `writeQueue = writeQueue.then(() => appendFile(...)).catch(() => {})` (swallow/log the write error) so a single failed append does not poison subsequent writes, and don't let a logging failure propagate into the smoke pipeline.

### L11. A throwing onDone shows a success toast then a contradictory error toast
- **File:** `apps/dashboard/lib/feedback.ts:85`
- **Category:** error-handling
- **Detail:** In runActionWithFeedback, the success branch first shows the success toast (line 83), then awaits opts.onDone(value) (line 85) inside the same .then. Because the .catch on line 87 is chained after this .then, any rejection thrown by onDone propagates into the .catch, which then ALSO shows an error toast (reusing the same pendingId, so it overwrites the success toast), sets the failure text via opts.setError, and console.warns. The net effect: a primary action that genuinely SUCCEEDED is reported to the user as a failure. The sibling helper runAction in api.ts (lines 197-214) documents this exact hazard ('a throwing onDone would leak ... and re-trigger the bug') but only protects against the unhandled-rejection; feedback.ts additionally mis-signals success as failure because it has already emitted the success toast before awaiting onDone.
- **Suggested fix:** Move the onDone await out of the toast-success path, or guard it: await opts.onDone in its own try/catch that does NOT re-show an error toast or call setError on the already-succeeded action (mirror the intent of runAction but suppress the duplicate failure UI). For example, run onDone before showing the success toast, or wrap it so a refresh failure logs only and does not flip the toast/error state.

### L12. readThreadSource open-redirect guard misses backslash / encoded-slash prefixes
- **File:** `apps/dashboard/lib/thread-source.ts:68`
- **Category:** security
- **Detail:** readThreadSource() validates a sessionStorage value before it is handed to router.push() at apps/dashboard/app/thread/[id]/page.tsx:2774-2781. The guard rejects empty, non-'/', and '//' (protocol-relative) prefixes and '/thread/'. It does NOT reject a single leading slash followed by a backslash (e.g. '/\\evil.com') or an encoded slash ('/%2Fevil.com'). Browsers normalise backslashes to forward slashes when resolving URLs, so router.push('/\\evil.com') resolves to '//evil.com', a protocol-relative URL that navigates the tab off-origin to evil.com. The module comment explicitly claims this validation 'closes the door on a malicious storage value being used as a router destination', so this is a gap in a security control that advertises completeness.
- **Suggested fix:** After the existing checks, reject any value whose second character is '\\' (and ideally normalise/decode before validation), or better, parse with new URL(stored, location.origin) and confirm the resolved origin equals location.origin before returning; otherwise return FALLBACK. e.g. add `if (/^\/[\\/]/.test(stored)) return FALLBACK;` to cover both '//' and '/\\'.



---

# Pass 2 — Deeper diverse-lens sweep (2026-06-06)

A second pass over current `origin/v1` (incl. the new #550 self-updater) using per-file security/concurrency/data-integrity/crash/logic/resource lenses, excluding everything pass 1 already covered. **45 confirmed new bugs** (13 High, 22 Medium, 10 Low).

## Merged to v1 (fix + regression test each)
- **10 Highs**: LinkedIn voice-guid routing (#559), iMessage timestamp drift (#561), profile-url scheme/host allowlist (#563), send atomic-claim crash-resend (#565), multipart path-traversal (#567), file-stream runner-crash guards (#569), thread-page cross-thread composer leaks (#573).
- **10 Mediums**: mic/spinner (#579), scan-queue metadata+abort (#582), voice-scope/name-label/reassess-canonical (#586), enrichment job lifecycle (#589), thread JSON safe-parse (#593).
- **2 Lows**: import-history db handle + favourite revert guard (#605).

## Open PRs (NOT auto-merged)
- **#574 / #575** — self-updater https-only gate + stop-app-before-DB-copy (for the #550 author / PR 2 of 2).
- **#591 / #595** — enrichment scan-lock serialization + session-manager teardown TOCTOU (concurrency changes, want a human review).

## Deferred to tracked issues
- **#553 / #554 / #555** (High) — self-updater unauth-channel RCE, torn-DB-copy, runner 0.0.0.0 bind.
- **#596 / #597** (Medium) — iMessage canonical-sibling display divergence (delicate #499 area; needs a careful dedicated fix).
- **#598–#602** (Medium) — #550 self-updater version-compare + minimumInstallerVersion enforcement.

## Logged Lows (not individually fixed)

### PL1. Favourite toggle failure path can set a stale favOverride onto the next thread after navigation  _( ✅ FIXED (#605) )_
- **File:** `apps/dashboard/app/thread/[id]/page.tsx:2321`  ·  **Lens:** state-race
- `toggleFavourite` (line 2315) captures `next` from thread A, optimistically sets `favOverride`, and on request failure calls `setFavOverride(!next)` (line 2321) with no check that the user is still on thread A. The threadId-change effect (lines 503-505) resets `favOverride` to null on navigation, but a late `.catch` that fires after navigating to thread B will write A's reverted favourite value into the shared `favOverride` state, which thread B's header star reads via `favOverride ?? thread.personFavourite` (line 2314). The star for B then briefly shows the wrong (A-derived) favourite state until the next refresh reconciles it.
- **Suggested fix:** Capture the thread id at toggle time and gate the `.catch` revert on it still matching the current route thread id (or store the override keyed by thread/person id) so a failed toggle for A cannot repaint B's star.

### PL2. /control/imessage/import-history leaks the chat.db IMessageDb handle on every call  _( ✅ FIXED (#605) )_
- **File:** `apps/runner/src/index.ts:1569`  ·  **Lens:** resource
- The POST /control/imessage/import-history route opens `db = new IMessageDb(runnerConfig.imessage.dbPath)` at index.ts:1569 and uses it via `db.listThreads(5000, ...)` at line 1577, but never calls `db.close()` anywhere in the rest of the handler (lines 1582-1662). There is no try/finally around the opened handle (the try at 1568-1573 only guards construction and re-throws nothing). The handle leaks on every code path: (a) the dryRun early-return at line 1605 returns after only listThreads; (b) the normal path runs the entire `withPlatformControlLock`/ingest loop and returns at 1654 with the handle still open; (c) if any await inside the loop or the lock body rejects, asyncRoute's rejection propagates and the handle is still never closed. Each invocation leaks one better-sqlite3 connection to chat.db plus its WAL read mark. This route is the ONLY one of the several IMessageDb call sites in this file that fails to close: the boot probe (index.ts:6340-6341), the /data/imessage-attachment route (1676/1699 try-finally), the attachment resolver (318/332 try-finally), the adapter (closeSession at imessage-adapter.ts:441), and birthday-sync (88/100 try-finally) all release the handle. Trigger: repeatedly POSTing to /control/imessage/import-history (e.g. a tester re-running history import, or any retry loop) accumulates open SQLite handles/file descriptors against ~/Library/Messages/chat.db until the process hits its fd limit or GC eventually finalizes them non-deterministically.
- **Suggested fix:** Wrap the handle in try/finally: after `db = new IMessageDb(...)` succeeds, put the listThreads call, dryRun branch, and the withPlatformControlLock ingest loop inside a `try { ... } finally { db.close(); }` so every return path (dryRun early-return, normal completion, and thrown errors inside the lock/loop) releases the chat.db connection — mirroring the pattern already used at index.ts:1681-1700 and 322-333.

### PL3. enqueue() coalescing is a check-then-act race: concurrent non-manual enqueues create duplicate PENDING jobs for the same person  _( logged — low impact )_
- **File:** `apps/runner/src/services/enrichment-queue.ts:141`  ·  **Lens:** concurrency
- enqueue() coalesces by doing `await prisma.enrichmentJob.findFirst({ where: { personId, status: { in: [PENDING, RUNNING] } } })` and, if none found, `await prisma.enrichmentJob.create(...)` (lines 142-149). There is no unique constraint on EnrichmentJob.personId (schema.prisma:183-205 has only a non-unique `@@index([personId])`), so this is a pure in-application check-then-act. The two `await`s straddle an event-loop yield point. Two enqueues for the same person that interleave there both observe 'no existing row' and both create a PENDING job. Concretely this is reachable today: the scan path fires `void enrichmentQueue.enqueue(personId, 'first_seen')` fire-and-forget (index.ts:734, via onNewPerson at scan-queue.ts:2782/2797), and the hourly periodicTick (line 453-455) also calls `await enqueue(p.id, 'periodic')` for active stale contacts. A scan discovering/auto-URLing a person while the periodic tick is iterating the same person (or two scans landing the same new person) interleaves at the findFirst/create boundary and yields two PENDING rows. The drain then visits the SAME LinkedIn profile twice, burning two slots of the daily cap (deps.dailyCap) that exists specifically to throttle this highest-fingerprint activity, and the dashboard 'Enriching N profiles' counter (index.ts:1337 counts PENDING jobs) over-reports.
- **Suggested fix:** Make coalescing atomic. Either add a partial/unique constraint so at most one non-DONE/FAILED job per personId can exist and catch the P2002 on create (treating it as 'already queued'), or perform the dedup inside a transaction with `SELECT ... FOR UPDATE`-equivalent semantics. An in-memory `Set<personId>` of in-flight enqueues guarding the await window would also close it for the single-process runner.

### PL4. Reassess classifier is fed the requested sibling's stale summary/whatTheyWant, not the canonical sibling's, for multi-handle iMessage persons  _( ✅ already fixed by PM11 (#586) )_
- **File:** `apps/runner/src/services/reassess-thread.ts:146`  ·  **Lens:** logic
- For an iMessage Person split across handle-specific sibling threads, runReassessForThread correctly (a) classifies over the MERGED sibling messages (orderedMessages, lines 129-135) and (b) targets the CANONICAL sibling for the summary refresh and cache burn (canonicalThreadId, lines 121-128, 140, 161). But the summary/whatTheyWant context lines passed to classifyThreadCategory (lines 146-147) are read from `thread.rollingSummary`/`thread.whatTheyWant`, where `thread` is the REQUESTED row fetched up front (lines 83-93). When Reassess is triggered on a non-canonical (dormant) sibling — e.g. via a notification deep-link that uses the inbound row's own threadId, a bookmarked/old thread URL, or a direct API call — `thread.rollingSummary` is the dormant row's stale summary, which neither the /data/thread reader nor the scan pipeline ever surface (both source AI fields from the canonical sibling via pickCanonicalThread, index.ts lines 3861-3866). classifyThreadCategory injects this stale 'Summary so far:' / 'What they want:' line into its prompt (ai.ts lines 2413-2426), so the classifier is biased by a summary that may not describe the merged live conversation, while every other input (merged messages) and the write target are canonical.
- **Suggested fix:** After resolving canonicalThreadId for the iMessage multi-sibling case, read the summary/whatTheyWant for the classifier from the canonical sibling row rather than the requested `thread`. The siblingRows query already runs (lines 117-120); add rollingSummary/whatTheyWant to its select and feed the canonical row's values (falling back to `thread`'s when single-sibling/non-iMessage) into classifyThreadCategory at lines 146-147.

### PL5. Row can render needsReply=true while riskLevel=GREEN and slaCountdown='No SLA' when lastInboundAt is null  _( logged — low impact )_
- **File:** `apps/runner/src/services/thread-row-shaping.ts:330`  ·  **Lens:** logic
- The row's needsReply field (line 330) comes from deriveNeedsReply(source), which for a row with lastInboundAt===null returns the stored DB column row.needsReply (line 218) — which can be true (e.g. AI summary.needs_reply=true, or a seeded thread). But riskLevel (line 329) and slaCountdown (line 339) come from calculateRisk, which for a null lastInboundAt returns level:'GREEN' and slaDueAt:undefined (risk.ts:20-27). Since needsReply is true, line 339 calls formatSlaCountdown(undefined) which returns 'No SLA' (risk.ts:64-65). Net: a row that is flagged needs-reply but shows GREEN risk and 'No SLA' — a self-contradictory state, and it survives the needsReplyOnly inbox filter (index.ts:3682) while never aging to amber/red.
- **Suggested fix:** Derive needsReply consistently with risk: when lastInboundAt is null, treat needsReply as false (no inbound = nothing owed), or compute needsReply from the same calculateRisk result used for riskLevel so the badge, risk colour and SLA countdown can't contradict each other.

### PL6. A dangling --notes (or --notes-file) flag publishes a latest.json the updater will refuse  _( ↳ deferred to #550 author )_
- **File:** `scripts/build-student-release.mjs:103`  ·  **Lens:** correctness
- parseArgs uses `next = () => argv[++i]` (line 53). A trailing `--notes` with no following value pushes `undefined` into out.notes (line 57). readNotes() then returns args.notes because `args.notes.length` is truthy (line 102), so releaseNotes becomes `[undefined]`, which JSON.stringify writes as `[null]` (verified). validateLatestJson in release-manifest.mjs flags `releaseNotes` whose members aren't strings (`obj.releaseNotes.some((n) => typeof n !== 'string')`), so loadManifest() in update-student.mjs dies with 'latest.json is malformed' for every pilot that fetches the feed. The `--notes-file` path is a milder variant: an empty or all-blank notes file yields `releaseNotes: []` (valid but empty — pilots see no 'what's new').
- **Suggested fix:** After building the notes/args, drop non-string/empty note entries (e.g. `out.notes.push(next())` guarded so undefined isn't pushed, and filter Boolean+typeof string in readNotes). Optionally run validateLatestJson on the manifest object before writing latest.json so the builder fails loudly instead of shipping a manifest the updater rejects.

### PL7. Temp staging directory leaks when the forbidden-file scan triggers die()  _( ↳ deferred to #550 author )_
- **File:** `scripts/build-student-release.mjs:231`  ·  **Lens:** correctness
- build() wraps its work in try/finally where the finally removes the staging dir (line 231 `rmSync(staging, ...)`). But die() (line 84) calls `process.exit(1)`, which terminates the process immediately and skips the finally block. The two in-try die() paths — the staged-tree forbidden scan (line 191) and the in-zip forbidden scan (line 206) — therefore leave the mkdtemp staging directory (line 170, containing a full extracted copy of the tracked source tree) behind in tmpdir on every failed build.
- **Suggested fix:** Don't process.exit from inside the try. Either throw from these checks and let the outer .catch (line 235) report+exit after the finally has run, or explicitly `rmSync(staging, { recursive: true, force: true })` immediately before each die() call in build().

### PL8. minimumInstallerVersion is required+validated in the manifest but never checked before applying, so an outdated updater will apply a build it cannot correctly handle  _( ↳ deferred to #550 author )_
- **File:** `scripts/update-student.mjs:314`  ·  **Lens:** security
- The manifest requires and validates minimumInstallerVersion (release-manifest.mjs lines 31, 97-101) and the updater imports compareVersions (line 44), but applyUpdate (called at 314/316) never compares the installer's own version against manifest.minimumInstallerVersion — compareVersions is imported and never used (confirmed by grep: only isNewer, sha256Buffer, validateLatestJson are referenced). A release that explicitly demands a newer updater (e.g. because the swap/preserve protocol changed) will still be applied by an old updater, which can mis-handle the new layout (wrong PRESERVE set, changed folder name) and, combined with the swap, risk data loss the gate exists to prevent.
- **Suggested fix:** Before applying, compute the running updater's version (from its own package.json/release.json) and refuse with a clear message when compareVersions(installerVersion, manifest.minimumInstallerVersion) < 0, instructing the pilot to update the updater/installer first.

### PL9. Non-numeric/missing --keep-backups value silently disables all backup pruning  _( ↳ deferred to #550 author )_
- **File:** `scripts/update-student.mjs:278`  ·  **Lens:** correctness
- parseArgs does out.keepBackups = Number(next()) (line 64). If --keep-backups is passed without a value or with a non-numeric value (e.g. `--keep-backups all`), Number(...) is NaN. At line 278, Math.max(0, NaN) is NaN, and pruneBackups' loop `while (backups.length > keep)` is `len > NaN`, which is always false, so no old backup is ever removed. Each backup is a full copy of the app directory, so backups then accumulate without bound across every update.
- **Suggested fix:** Validate the parsed number: `const keep = Number.isFinite(args.keepBackups) ? Math.max(0, Math.floor(args.keepBackups)) : 2;` and pass that to pruneBackups; die() on an explicitly malformed --keep-backups value.

### PL10. --dry-run on an up-to-date install prints a contradictory 'would update X → X'  _( ↳ deferred to #550 author )_
- **File:** `scripts/update-student.mjs:315`  ·  **Lens:** correctness
- In main(), `--dry-run` without `--apply` always routes into applyUpdate regardless of whether an update is available (line 315-317: `else if (args.dryRun) await applyUpdate(...)`). applyUpdate's dry-run branch (line 194) unconditionally prints `[dry run] would update <current> → <manifest.version>` and lists download/verify/swap steps. When latest == current (or older), report() has just printed 'You're up to date', and the dry-run then prints 'would update 0.1.0 → 0.1.0' plus a full plan to download and swap, directly contradicting it. Concrete trigger: `node scripts/update-student.mjs --dry-run` against a feed whose version equals the installed version.
- **Suggested fix:** Gate the standalone dry-run on availability too: only call applyUpdate for --dry-run when `report` returned available (or when --apply is also set), otherwise print the same 'nothing to do' message report() already shows.



---

# Pass 3 — Scoped long-tail diverse-lens sweep (2026-06-06)

Scoped pass over the 117 long-tail files (dashboard lib/components, packages/core, runner scripts) that earlier passes only general-lensed, with one best-fit lens each (state-race / logic / data-loss). 35 confirmed (3 High, 19 Medium, 13 Low).

## Merged to v1
- **3 Highs** (destructive scripts, fail-closed): tccutil confirm-guard, reassess skip-overwrite-on-AI-fallback, repair-linkedin preserve Draft/SendRequest (#613, #627).
- **17 Mediums**: destructive-script guards + scoped reset (#613); dashboard component state-races (#618); dashboard-lib logic (#625); reassess fallback (#627); notification-CTA re-sync + autocorrect abbreviations (#631); voice-profile edit races (#634).
## Deferred to issue
- **#628** (Medium) overdue-digest UTC-vs-local cadence — cross-package date fix needs careful dedicated work (auto-fix's own test failed).

## Logged Lows (not individually fixed — low impact)

### PL1. startTour's post-await branch reads the shared skipPendingRef and writes bootstrapping:false from a stale invocation, un-gating a newer tour before its sandbox is seeded
- **File:** `apps/dashboard/components/common/PilotTour.tsx:127` · **Lens:** state-race
- startTour resolves its sandbox POST and then, in the same closure, reads skipPendingRef.current (line 122) and otherwise calls setState((prev) => prev.active ? { ...prev, bootstrapping: false } : prev) (line 127). skipPendingRef is a single shared ref and the post-await setState guards only on `prev.active` (true for ANY running tour), with no token identifying which startTour invocation is resolving. Sequence: (1) startTour A sets bootstrapping:true and awaits startPilotSandbox() (POST A in flight); (2) operator presses Skip while bootstrapping → skipTour takes the defer-teardown branch, sets skipPendingRef.current=true and setState({active:false}) (lines 89-94); (3) operator triggers a restart (Settings 'Replay walkthrough', or the still-mounted Today invite card per the sibling finding) → startTour B runs, line 105 resets skipPendingRef.current=false (dropping A's deferred skip) and awaits a SECOND startPilotSandbox() (POST B in flight); (4) POST A now resolves inside A's closure: shouldTearDownDeferredSkip(skipPendingRef.current) is false (B cleared it), so it falls to line 127, sees prev.active===true (tour B), and writes bootstrapping:false — clearing tour B's bootstrapping flag while POST B is still in flight and B's sandbox seed has not landed.
- **Suggested fix:** Tag each startTour invocation with a token (e.g. a module/ref counter captured as a local const at entry) and, before the line 122 ref read and the line 127 setState, bail if the token is no longer current. Equivalently, guard re-entry (see sibling finding) so two startTour runs can never overlap and share skipPendingRef.

### PL2. Stale query/selection state from the previous open is committed for one frame on reopen
- **File:** `apps/dashboard/components/layout/command-palette.tsx:41` · **Lens:** state-race
- Closing the palette (Esc / background click) only flips `open` to false in the parent; the `[open]` effect's early return `if (!open) return` (line 42) means `query`, `activeIndex`, and `threads` are never cleared on close. The component is mounted once for the whole app session (it lives in the root `AppShell` in app/layout.tsx and only returns null when closed at line 114), so that state persists. The reset to a clean state (`setQuery("")`, `setActiveIndex(0)`, line 43-44) happens inside the `[open]` effect, which runs AFTER the reopen render commits. So when the user reopens with Cmd-K, the first committed frame renders the input still showing the previous session's typed query, the previously filtered `items`, and the stale `activeIndex` highlight, before the effect clears them on the next tick. The user sees a flash of their last search rather than a clean empty palette.
- **Suggested fix:** Clear the search state on close instead of (or in addition to) on open: either reset `query`/`activeIndex` in the parent's onClose, or compute the open-state reset during render rather than in a post-commit effect (e.g. derive `query`/`activeIndex` from a key that resets when `open` toggles, or remount the inner panel with `key={open ? 'open' : 'closed'}` so a fresh open always starts blank).

### PL3. errored state is never reset when avatarUrl prop changes, defeating the documented "next scan refreshes the row" recovery
- **File:** `apps/dashboard/components/common/person-avatar.tsx:38` · **Lens:** state-race
- PersonAvatar keeps a single state flag `errored` (line 14), set by the <img> onError handler (line 38), and computes `showImage = Boolean(avatarUrl) && !errored` (line 15). Once `errored` flips to true it is never cleared: there is no effect resetting it on prop change and no `key` on the <img> tied to the URL. So when the SAME mounted instance later receives a DIFFERENT, valid `avatarUrl`, the stale `errored=true` keeps `showImage=false` and the new image is never attempted. The component is rendered inside thread-row.tsx / selectable-thread-row.tsx lists that are keyed by stable `row.id` (e.g. inbox/page.tsx key={row.id} at lines 807/822) and polled in place every 10s (inbox/page.tsx useVisiblePolling(... , 10000) at line 295). The runner re-derives personAvatarUrl fresh from person.avatarUrl on each fetch (thread-row-shaping.ts:494, index.ts:4485/4992/5038), and LinkedIn avatar URLs are signed and rotate, so a still-mounted row's avatarUrl genuinely changes value across polls. This directly defeats the recovery the file's own comment promises at lines 27-29 ("onError falls through to the initials tile until the next scan refreshes the row").
- **Suggested fix:** Tie the error slate to the URL so a changed avatarUrl gets a fresh attempt. Simplest: add `key={avatarUrl ?? ""}` to the <img> (line 31) so a new URL remounts the img element and re-arms onError, OR reset state on URL change with an effect, e.g. `const lastUrl = useRef(avatarUrl); useEffect(() => { if (avatarUrl !== lastUrl.current) { lastUrl.current = avatarUrl; setErrored(false); } }, [avatarUrl]);`. The key approach is preferable here (no extra render) and makes the documented recovery actually work.

### PL4. acceptFiles reads stale screenshots.length under concurrent drops, silently dropping over-cap images with no error
- **File:** `apps/dashboard/components/common/pilot-feedback-modal.tsx:108` · **Lens:** state-race
- acceptFiles (lines 103-143) is a useCallback closed over screenshots.length (dep at line 142). It reads screenshots.length once at the top to compute `room` (line 108) and to decide whether to surface the over-cap error (lines 109-112, 134-135), then it AWAITS readFileAsDataUrl for every file (line 122) before committing with the functional updater setScreenshots(prev => [...prev, ...picked].slice(0, MAX_SCREENSHOTS)) (line 138). Because the FileReader read is async, two near-simultaneous invocations (e.g. the user drops a batch onto the dropzone and, while the first batch is still being read, drops a second batch — or drops then immediately uses the file picker) both capture the SAME pre-update screenshots.length. Concrete trigger with MAX_SCREENSHOTS=4 and 0 attached: invocation A drops 3 files -> room=4, `3 > 4` false, no error; invocation B (fired before A's setState commits) also sees length 0 -> room=4, `3 > 4` false, no error. The two functional updaters run sequentially on the committed array (0 -> +3 = 3 -> +3 = 6) and .slice(0,4) caps it at 4, so 2 picked images are silently discarded and the user is never told (neither invocation set screenshotError because each individually passed the `picked.length > room` check). The .slice prevents corruption/over-cap, so this is data-drop-with-no-feedback rather than a crash.
- **Suggested fix:** Do the capacity accounting inside the functional updater so it sees the freshest committed array. E.g. compute `room` and the over-cap condition against `prev` inside setScreenshots(prev => {...}), and set screenshotError via a flag derived there (or move the error into a ref/state set after the updater). Minimal correct fix: in the updater, `const merged = [...prev, ...picked]; if (merged.length > MAX_SCREENSHOTS) setScreenshotError(\`You can attach up to ${MAX_SCREENSHOTS} images.\`); return merged.slice(0, MAX_SCREENSHOTS);` so over-cap is detected against the true current length rather than the stale captured one.

### PL5. Reconnect modal stays open showing an empty body after the last degraded platform reconnects
- **File:** `apps/dashboard/components/layout/top-status.tsx:593` · **Lens:** state-race
- `reconnectOpen` is independent boolean state (line 317), while the modal body is rendered by mapping over `degradedPlatforms` (line 614), which is derived fresh on every render from the polled `platforms` snapshot (`degradedPlatforms = implemented?.filter(p => p.status !== "CONNECTED") ?? []`, line 404). When the operator opens the modal with one degraded platform and clicks "Reconnect", `runPlatformAction` (lines 407-422) succeeds and then calls `refreshRef.current()` (line 414), which re-fetches `/runner/data/platforms` and flips that platform's status to CONNECTED. On the next render `degradedPlatforms` becomes `[]` and `hasDegraded` becomes false, but `reconnectOpen` is still `true` because nothing closes it. The modal then renders the header "Connection issue" plus the body text "These platforms aren't connected. Reconnect, or reset the session if reconnect keeps failing." over an empty platform list — a self-contradictory, dead-end state until the operator manually clicks Close. The same desync occurs if a background 5s poll (or a `runner-event` refresh) is what observes the platform recovering while the modal is open. This is the 'optimistic-update / poll desync' failure mode: the modal's open-state and the data that justifies it are tracked separately and drift apart.
- **Suggested fix:** Auto-close the modal when there is nothing left to reconnect. Add an effect: `useEffect(() => { if (reconnectOpen && !hasDegraded) setReconnectOpen(false); }, [reconnectOpen, hasDegraded]);` — or, in the modal body, render a brief "All platforms reconnected." success state with the Close button when `degradedPlatforms.length === 0` instead of an empty list. Closing on success also gives the operator clear confirmation the reconnect worked.

### PL6. subscribeNotificationPermission cleanup races the async permissions.query, leaking a 'change' listener on fast mount/unmount
- **File:** `apps/dashboard/lib/notifications.ts:101` · **Lens:** logic
- subscribeNotificationPermission registers the navigator.permissions 'change' listener inside an async `.then` (line 92-95): `permissionStatus = status; status.addEventListener('change', handler)`. The returned cleanup (lines 101-104) removes the focus listener synchronously and calls `permissionStatus?.removeEventListener('change', handler)`. But `permissionStatus` is only assigned when the query promise resolves. If the caller unmounts (runs the cleanup) BEFORE `permissions.query(...)` resolves, `permissionStatus` is still null, so the cleanup's optional-chained removeEventListener is a no-op. The promise then resolves and adds a 'change' listener that nothing will ever remove — and `handler` closes over the now-stale `onChange`.

Concrete trigger: a component (e.g. the settings notification toggle / AppShell) mounts and unmounts quickly, or React Strict Mode mounts-unmounts-remounts on first render, within the microtask/event-loop window before permissions.query resolves. Each such cycle leaks one persistent PermissionStatus 'change' listener bound to a dead closure; on the next permission flip every leaked handler fires, invoking stale onChange callbacks (state updates on unmounted components / duplicate work).
- **Suggested fix:** Guard the async branch against a cleanup that already ran. Track a `cancelled` flag in the closure: set it true in the returned cleanup; in the `.then`, if `cancelled` is true, do not addEventListener (or immediately remove it). E.g. `let cancelled = false; ... .then(status => { if (cancelled) return; permissionStatus = status; status.addEventListener('change', handler); }); return () => { cancelled = true; window.removeEventListener('focus', handler); permissionStatus?.removeEventListener('change', handler); };`

### PL7. initials() splits Supplementary-Plane (astral) letters, producing a broken glyph
- **File:** `apps/dashboard/lib/risk.ts:41` · **Lens:** logic
- initials() is deliberately Unicode-aware: line 38 uses the regex /^\p{L}/u (with the `u` flag) so a name token that starts with a letter is selected for the initial, and the file comments call out accented-name handling ("José"). But the character extraction on line 41, `word.map((word) => word[0] ?? "")`, uses plain UTF-16 string indexing, which is NOT code-point aware. For a token whose first letter lives in the Supplementary Plane (code point ≥ U+10000 — e.g. Mathematical Alphanumeric Symbols like '𝓙ohn', or CJK Extension-B+ surnames such as '𠀀'), `\p{L}/u` correctly classifies the token as letter-led so it passes the filter, but `word[0]` returns only the first UTF-16 code unit — a lone high surrogate (e.g. '\ud840', '\ud835'). The avatar then renders a broken/replacement glyph instead of the intended letter. Verified by running the function: `initials("𠀀 Wong")` returns "\ud840W" and `initials("𝓙ohn")` returns "\ud835". The same flaw hits the no-letter fallback branch (parts.slice(0,1)): `initials("🎉")` returns "\ud83c" rather than a clean character.
- **Suggested fix:** Extract the first code point instead of the first UTF-16 unit. Replace `word[0] ?? ""` with something code-point aware, e.g. `[...word][0] ?? ""` (spread iterates by code point) or `Array.from(word)[0] ?? ""`. This keeps the existing \p{L}/u logic consistent end-to-end and renders a single complete glyph for Supplementary-Plane letters.

### PL8. Shared module-level timer lets one row's onMouseLeave cancel another row's pending prefetch (keyboard-focus + mouse interleave)
- **File:** `apps/dashboard/lib/thread-prefetch.ts:28` · **Lens:** logic
- The module keeps a single module-global `timer` (line 17), and `cancelThreadPrefetch()` (lines 28-33) cancels it unconditionally with no reference to which row scheduled it. Every row component wires BOTH `onFocus={() => prefetchThreadData(row.id)}` and `onMouseLeave={cancelThreadPrefetch}` (thread-row.tsx:73-75, selectable-thread-row.tsx:169-171, inbox/page.tsx:1137-1139), so keyboard focus and mouse hover are two independent, simultaneously-live input paths into the same single timer.

Concrete trigger: (1) user Tabs to Row A -> onFocus(A) schedules the 80ms timer for A; (2) within that 80ms the mouse, resting over a different Row B, moves and leaves B -> onMouseLeave(B) fires cancelThreadPrefetch(), which clears A's pending timer; (3) A's prefetch never fires. Pressing Enter on A then pays the cold /runner/data/thread round-trip the module exists to eliminate. The module's own docstring assumption — "only one row is hovered at a time" (lines 12-15) — holds for pure-mouse use but is violated by the mouse+keyboard interleave the rows explicitly support (they bind onFocus). Note there is also no onBlur cancel, so a focus-scheduled prefetch is only ever cancelled by some other row's mouse-leave, which is exactly the cross-row coupling above.
- **Suggested fix:** Make cancellation row-aware so an unrelated row can't cancel a live prefetch: store the scheduled threadId alongside the timer and have cancelThreadPrefetch accept the row id, only clearing when it matches the pending one (e.g. `cancelThreadPrefetch(id?: string)` that returns early if `id && id !== pendingId`); wire onMouseLeave/onBlur to pass row.id. Alternatively key the debounce per-thread (Map<threadId, timer>) so cancels target the right entry. Severity is Low because the failure mode is only a missed prefetch (the click falls back to the normal fetch the docstring already accepts).

### PL9. sortTodayQueue sorts a null lastInboundAt to the FRONT of its risk bucket (epoch 0), inverting the documented "oldest-waiting first" intent and contradicting the sibling digest sort
- **File:** `apps/dashboard/lib/today.ts:46` · **Lens:** logic
- sortTodayQueue's within-bucket tie-break is `const aIn = a.lastInboundAt ? Date.parse(a.lastInboundAt) : 0` / `bIn = b.lastInboundAt ? ... : 0` (lines 46-47), then `return aIn - bIn` (oldest first). A row whose `lastInboundAt` is null is therefore treated as timestamp 0 (1970) and sorts to the FRONT of its risk bucket — i.e. as the single most-overdue thread. This row is genuinely reachable: `isInTodayQueue` only requires `needsReply !== false` (today.ts:17), and the runner's row shaper derives needsReply via `deriveNeedsReply` (thread-row-shaping.ts:253-258), which when `lastInboundAt` is null falls back to the persisted `needsReply` DB column — which can be true (seeded/legacy/retracted-inbound rows; this exact contradictory state is logged as PL5). Such a row also gets `riskLevel:"GREEN"` from calculateRisk (risk.ts:20-26), so it lands in the GREEN bucket. In a Today queue that is currently all-GREEN (the operator has cleared the RED/AMBER threads — a common state), this null-inbound row jumps ahead of every legitimately fresh GREEN thread that has a real recent inbound timestamp and becomes `sortedRows[0]`, which page.tsx:342 promotes to the Today hero. The analogous sibling sort in overdue-digest.ts:248-249 deliberately does the OPPOSITE for the identical situation: `a.lastInboundAt ? Date.parse(a.lastInboundAt) : Number.MAX_SAFE_INTEGER`, pushing a missing/unknown inbound to the BACK of the queue. Both sorts document the same "RED before AMBER, then oldest lastInboundAt first" rule, but today.ts treats "unknown waiting time" as maximally-overdue while the digest treats it as least-overdue. today.ts's choice is the wrong one for its own stated intent: a missing inbound timestamp is not an old inbound, and surfacing it as the hero misranks the screen.
- **Suggested fix:** Mirror overdue-digest.ts: default a missing lastInboundAt to the END of its bucket, not the start — `const aIn = a.lastInboundAt ? Date.parse(a.lastInboundAt) : Number.MAX_SAFE_INTEGER` (and same for bIn). Also guard against a non-null but unparseable lastInboundAt (Date.parse → NaN makes the `aIn - bIn` comparator return NaN, an inconsistent comparator that yields undefined sort order): coerce NaN to MAX_SAFE_INTEGER as well, e.g. `const ts = a.lastInboundAt ? Date.parse(a.lastInboundAt) : NaN; const aIn = Number.isFinite(ts) ? ts : Number.MAX_SAFE_INTEGER;`. This keeps real oldest-waiting threads leading the bucket and pushes unknown-timestamp edge rows to the back, matching the digest and the function's own comment.

### PL10. Exclamation-density signal is broken: corpus strips sentence-terminating '!' but draft counts them, so the comparison is inverted
- **File:** `apps/dashboard/lib/voice-score.ts:84` · **Lens:** logic
- The exclamation signal compares corpus.exclamationsPerSentence against the draft's exclamations-per-sentence, but the two are computed over different text and are not comparable.

Corpus side (buildCorpusStats, lines 80-86): exclamations are counted by iterating the pieces returned from splitSentences(sample). splitSentences uses SENTENCE_RE = /[.!?]+\s+|[.!?]+$/ as the .split() delimiter (line 45), which CONSUMES the terminating '.', '!', '?'. So every '!' that ends a sentence is removed before line 84 (`sentence.match(/!/g)`) ever runs. For a normal corpus full of exclamatory messages ('Thanks so much!', 'Great to hear! See you soon!'), this yields exclamationsPerSentence ~= 0 — only rare mid-token '!' (e.g. an '!' not followed by whitespace and not at end) is ever counted.

Draft side (scoreDraftAgainstCorpus, line 108): `(trimmed.match(/!/g) ?? []).length / sentences` counts ALL '!' in the raw draft, terminators included.

Result: corpus.exclamationsPerSentence is systematically ~0 even for an exclamation-heavy operator, while draftExclamations reflects the real count. A perfectly on-voice draft with one '!' per sentence gives exclamationDelta ~= 1 (> 0.5 at line 149), so it incurs a penalty (line 150) AND emits the wrong signal 'More exclamation marks than your voice' (lines 153-155) — the opposite of the operator's actual habit. The signal can essentially never report 'Less exclamation than your voice' correctly, and it can never recognise a high-exclamation voice as on-voice.

Verified by reproduction: corpus-style counting of 'Thanks so much!' = 0 excl/sentence; 'Wow! Amazing! Incredible!' = 0; whereas the draft-style count of the same strings = 1.0 per sentence. (The parallel emoji signal is NOT affected: emoji are not in SENTENCE_RE, so splitSentences does not strip them and both sides count emoji identically — confirmed.)
- **Suggested fix:** Make the two paths count exclamations over the same text. Simplest fix: count '!' on the whole sample in buildCorpusStats the same way the draft does, instead of on post-split sentence pieces. E.g. replace the per-sentence loop accumulation at line 84 with `totalExclamations += (sample.match(/!/g) ?? []).length;` computed once per sample (outside the sentence loop), mirroring line 108. Equivalently, change the draft side to strip terminators first — but matching the draft to the corpus is cleaner since the draft already represents the intended 'all marks' semantics. Apply the same review to any other signal that relies on splitSentences pieces for character/punctuation counts (sentence-length is consistent because both sides sum splitSentences piece lengths).

### PL11. Over-broad name slot suppresses genuine messages that end with "...kept an audio message[ from you]"
- **File:** `packages/core/src/imessage-system-events.ts:26` · **Lens:** logic
- The KEPT_AUDIO_PATTERNS use `[^\n]{1,80}?` as the `<name>` slot, but that class matches ANY text (whole clauses, not just a name). Combined with the `^...\.?$` anchors, a real inbound message whose text is a complete sentence (<=80 chars before the phrase) ending in the canonical wording is matched and treated as a non-content system event. Verified examples that wrongly return true: "I cannot believe you kept an audio message." (pattern at line 31), "No way she kept an audio message." (line 31), "lol yeah he kept an audio message" (line 31), "Aw glad you kept an audio message from you." (line 26). Because callers use isNonContentIMessageSystemEvent to drop messages, such a turn is hidden from the thread view (apps/dashboard/app/thread/[id]/page.tsx:1977, apps/runner/src/index.ts:4530), excluded from AI prompts/needsReply via isAiVisibleMessage (apps/runner/src/services/ai.ts:555), and dropped at ingestion (apps/runner/src/platforms/imessage-adapter.ts:149). The file's own doc-comment (lines 20-22) sets the invariant that a false positive "silently hides a needs-reply turn, which is much worse than a false negative" and offers only ONE guard example (a phrase mid-sentence with trailing text), which passes; but sentences that END on the phrase are not guarded and slip through.
- **Suggested fix:** Tighten the `<name>` slot so it cannot swallow a multi-word clause/sentence. Options, in order of safety: (a) restrict the prefix to a plausible contact-name shape, e.g. 1-3 whitespace-separated tokens with no leading lowercase filler, instead of `[^\n]{1,80}?`; (b) require the prefix to be free of sentence-ending punctuation and limit token count; or (c) since chat.db emits these notices with a known sender, prefer matching against the structured sender/handle plus a fixed suffix rather than free-text. At minimum, anchor the bare-phrase patterns (lines 31, 33) more strictly because they are the easiest to trip. Keep the dashboard copy (apps/dashboard/lib/imessage-system-events.ts:19-22) in lockstep and extend tests/dashboard-imessage-system-events.test.mjs with the false-positive sentences above.

### PL12. mergeNotes silently drops a duplicate's notes when they are a substring of the canonical's notes, before the duplicate Person is irreversibly deleted
- **File:** `apps/runner/src/scripts/dedupe-imessage-people-plan.ts:162` · **Lens:** data-loss
- mergeNotes() is the only thing that preserves a duplicate Person's `notes` before the executable (dedupe-imessage-people.ts:99) runs `tx.person.delete({ where: { id: dupId } })`. Its docstring promises it merges "without ever dropping content." But line 162 — `if (existing.includes(incoming)) return canonicalNotes` — treats the duplicate's note as already-present whenever it appears anywhere in the canonical's notes AS A SUBSTRING, not only when it is the canonical's note. Concrete trigger: canonical Person's notes = "Met at the conference, follow up re funding"; the duplicate (same phone-key, e.g. "+44 7700 900123" vs "07700900123") carries notes = "follow up". `existing.includes("follow up")` is true, so mergeNotes returns the canonical unchanged, the apply loop then deletes the duplicate row (line 99), and that distinct note is gone with no copy anywhere. The planner always lists every duplicate in `duplicateIds` (plan file line 90) irrespective of notes, so the deletion still happens. The exposure widens with 3+ members in one handle group: the loop accumulates d1's notes onto the canonical first, so a later duplicate d2 whose short note ('client', 'follow up', a phone fragment) coincidentally became a substring of the now-larger canonical note is also dropped. This is real but narrow: it only loses operator-authored Person.notes (never threads/messages/enrichment), and only when one note is a literal substring of the other — which is also the same check that gives the function its desirable re-run idempotency, so the fix must distinguish 'already the same note' from 'coincidentally a substring'.
- **Suggested fix:** Tighten the suppression so it only no-ops on a genuine re-run (the incoming note already present as the merged block or as the whole canonical), not on any coincidental substring. E.g. replace `if (existing.includes(incoming))` with a check that the incoming text equals the canonical OR already appears as a previously-appended '--- merged from duplicate ---' block, and otherwise always append. Concretely: keep the equality/whole-block guard but, when incoming is merely a substring of a larger note, still append it under the divider. Add a regression test for `mergeNotes('alpha follow up beta', 'follow up')` asserting the 'follow up' note survives in the result.

### PL13. "empty" classification ignores aiHelpLevel/setupCompletedAt, so a profile with a deliberately-set AI level is silently overwritten without --force
- **File:** `apps/runner/src/services/operator-profile-seed.ts:54` · **Lens:** data-loss
- classifyOperatorProfile() returns "empty" (line 54) when all five text fields (displayName/about/interests/commonPhrases/avoidedPhrases) and preferredStyle are blank. It never inspects aiHelpLevel or setupCompletedAt. A real operator who completed first-run setup and explicitly set aiHelpLevel to a non-default value (e.g. "full_drafts", which turns on AI auto-drafting) but left the free-text voice fields blank produces exactly this state: empty text + preferredStyle:"" + aiHelpLevel:"full_drafts" + setupCompletedAt set. classifyOperatorProfile returns "empty", so shouldSeedOperatorProfile() (line 79) returns true even without --force. Then seed-operator-profile.ts line 83 calls store.updateOperatorProfile(seed) and overwrites aiHelpLevel and setupCompletedAt with the seed file's values. The upsert (settings.ts line 257) discards the previous valueJson, so the operator's deliberate AI-help choice and setup timestamp are lost irreversibly with no --force and no warning. The script's own docstring (lines 6-8) promises a profile is only replaced when 'still empty or a placeholder/test profile' and 'left untouched unless --force is passed', so this diverges from the documented contract.
- **Suggested fix:** Treat a non-default aiHelpLevel or a non-empty setupCompletedAt as evidence the profile is not truly empty. In classifyOperatorProfile, only return "empty" when, in addition to the blank text fields and preferredStyle, aiHelpLevel is the default (DEFAULT_AI_HELP_LEVEL) and setupCompletedAt is blank; otherwise fall through to the placeholder/real check (a deliberately-configured-but-text-blank profile then classifies as "real" and is preserved unless --force).



# Pass 4 — Cross-cutting integration sweep (2026-06-06)

Passes 1-3 swept files in isolation. Pass 4 fanned out dynamic-workflow agents over the **interactions between files** — producer/consumer contracts, multi-row identity, SSE event routing, cache-key parity — the class of bug a per-file lens cannot see. 16 High/Medium confirmed, all fixed; 3 Lows logged below.

## Merged to v1 (fix + regression test each)

- **#637** `fix(imessage): target the canonical sibling for AI writes, SSE routing, and the predraft cache key` — one cohesive PR closing the 11 canonical-sibling findings X1–X11. #499 moved the /data/thread READ path onto the canonical (most-recent-inbound) sibling of a multi-handle iMessage Person; the WRITE target, the THREAD_UPDATED SSE routing, the /predraft cache key, the scheduled-send join, and the risk computation still keyed off the requested/per-handle row, so they desynced (a send-side reassess wrote the fresh brief to the dormant row; a new inbound on the other handle never refetched the open view; predraft warmed a key the reader could never hit). Centralised on a new pure `canonicalWriteTargetId` helper + a sibling-aware `shouldRefetchForThreadEvent`, with `siblingIds` added to the ThreadResponse contract. Closes #636.
- **#639** `fix(presenter): runner-guard mark-all-for-reassess and /system/update in read-only demo` — findings X13, X14. Two real-state mutations had no server-side presenter guard: `/control/threads/mark-all-for-reassess` (a recovered tab whose localStorage interceptor is gone skips the client guard) and `/system/update` (not a `/control/` path, so the interceptor never sees it). Both now go through `checkPresenterGuard`; the dashboard folds the read-only sentinel into an explicit "read-only demo, nothing changed" outcome. Closes #638.
- **#641** `fix(linkedin-voice): route li-msg-fp: voice guids to the LinkedIn endpoint on both consumer sides` — finding X16. The adapter persists voice notes under `li-msg-fp:` / `li-msg-` keys for id-less bubbles, but the dashboard media component and the runner's serve-route zod gate only accepted `urn:li:`, so those notes never played (routed to the iMessage endpoint / rejected 400). Both consumers now use the canonical `isLinkedInVoiceGuid` predicate (runner's existing one; a lockstep dashboard copy). Closes #640.
- **#643** `fix(sse): prefer Last-Event-ID header over stale sinceEventId on reconnect` — finding X15. The /events handler resolved the resume cursor as `Number(query.sinceEventId ?? header ?? 0)`, so the mount-time query param (frozen at AppShell mount) beat the live `Last-Event-ID` header on the browser's native auto-reconnect, replaying the entire buffered window and re-triggering the dashboard refetch fan-out every reconnect. Extracted `resolveSseResumeCursor` to prefer the header. Closes #642.
- **#645** `fix(reconnect): surface disabled_by_settings on the Refresh AI scores button` — finding X12. The Reconnect "Refresh AI scores" button typed the runner response as `ok | ai_unavailable`, so the `disabled_by_settings` short-circuit (AI tier memory_only) fell through to a neutral "Scored 0", implying the scorer ran when AI is off. Mirrored the Inbox sibling via a pure `interpretRefreshScoresResult` helper ("AI is off (Settings)" + warn tone). Closes #644.

## Logged Lows (not individually fixed — low impact)

### P4L1. Inbox list ignores THREAD_UPDATED / MESSAGE_SENT / SCAN_FINISHED that Today consumes — stale up to the 10s poll
- **File:** `apps/dashboard/app/inbox/page.tsx:286` · **Lens:** event emitter/consumer asymmetry
- The runner fans out THREAD_UPDATED (incl. the reassess-on-send path), MESSAGE_SENT, and SCAN_FINISHED to the browser as `runner-event` window events. The Today page subscribes to `runner-event` and refetches on these; the Inbox page registers ONLY a `runner-resync` listener (RESYNC_REQUIRED) plus a 10s `useVisiblePolling` tick — it never subscribes to `runner-event`. Net effect: when a scan finishes, or the operator sends/reassesses a reply from the thread page or another tab, the open Inbox shows stale preview / whatTheyWant / needsReply / risk for up to ~10s (longer in a throttled background tab) while Today reflects it near-instantly. No wrong persisted data; self-heals on the next poll, hence Low.
- **Suggested fix:** Mirror Today's subscription — add a `runner-event` listener in inbox/page.tsx that calls the existing debounced `refresh()` on THREAD_UPDATED / MESSAGE_SENT / MESSAGE_SEND_FAILED / SCAN_FINISHED (debounced so a multi-thread scan's burst collapses into one refetch), keeping the 10s poll as a backstop.

### P4L2. Runner reconnect-scoring query and dashboard isReconnectCandidate disagree on the candidate set: the runner scores dormant threads with a queued scheduled send that the dashboard then hides
- **File:** `apps/runner/src/index.ts:2122` · **Lens:** producer/consumer predicate divergence
- The reconnect-candidate predicate exists twice with different conditions. The dashboard's `isReconnectCandidate` excludes a thread when `scheduledSendAt` is set (a queued reply removes it from the Reconnect page). The runner's refresh-scores DB query filters only on `archivedAt: null`, dormant `lastMessageAt`, and `NOT category: outreach` — no exclusion for a SCHEDULED SendRequest. So the runner spends an AI call scoring (and persists `reconnectScore`/reason/cacheKey onto) a dormant thread the dashboard never shows. Wasted provider budget + a persisted score for an invisible row; no data corruption and no wrong on-screen value, hence Low.
- **Suggested fix:** Make the runner query mirror the dashboard's `scheduledSendAt` exclusion — fetch the SCHEDULED sendRequest threadIds first (as /data/inbox already does) and add `NOT: { id: { in: scheduledThreadIds } }`, or filter them out before `scoreReconnectCandidate`. Keep the dashboard predicate authoritative.

### P4L3. Overdue-digest notification fires before the runner ack is confirmed; a failed/rejected ack re-fires the same notification every 5 minutes with lastDigestAt never advancing
- **File:** `apps/dashboard/components/layout/app-shell.tsx:362` · **Lens:** cross-boundary transaction ordering
- The AppShell digest scheduler fires the desktop notification first, then POSTs `/control/overdue-digest/ack`, swallowing failure with `.catch(() => undefined)`. The runner ack is the ONLY thing that advances `lastDigestAt` and writes per-person `lastIncludedAt`. If the ack fails (network blip, or a 409 `cadence_off` because the operator flipped cadence off between tick and ack), `lastDigestAt` never advances; the next 5-minute poll still reports due with the same candidates and re-fires. The stable notification tag (`inbox-os:overdue-digest`) means the OS replaces rather than stacks, so the user sees one notification silently re-asserting every 5 minutes rather than a pile. Self-resolves once an ack succeeds, hence Low.
- **Suggested fix:** Treat ack as part of the fire transaction — await it and, on failure, set an in-memory "already fired this digest" guard (keyed by lastDigestAt + localDate or the candidate set) so the scheduler does not re-fire the identical digest before a successful ack lands. Distinguish a 409 cadence_off (stop trying this window) from a transient failure (retry the ack without re-firing).
