node_modules
.npm
.turbo
.next
dist
coverage
/build/windows-runtime/
# Student release artefacts (zips, latest.json, checksums) + backups the
# updater makes. Never commit built release zips — rebuild them.
/release-dist/
/.release-signing/
/.update-backups/
release.json
# Local publishing config: Dropbox token + stable URLs. NEVER commit.
.env.release.local
.DS_Store
.env
.env.local
apps/dashboard/.next
apps/dashboard/out
apps/runner/dist
packages/core/generated
*.tsbuildinfo

# Data runtime state
/data/
/logs/runs/
/logs/*.log
/apps/runner/logs/runs
/apps/runner/LATEST_LINKEDIN_SCAN.txt
/apps/runner/logs/runs/LATEST_LINKEDIN_SMOKE.txt
/apps/runner/logs/**/*.trace
/apps/runner/logs/**/*.ndjson
/apps/runner/logs/**/*.csv

# Local tooling state (not part of the project)
/.claude/
/.codex-disabled-*/
# Impeccable design-skill machinery installed into the GitHub-Copilot harness
# folder. Local tooling, like /.claude; the design *context* lives in the
# committed PRODUCT.md / DESIGN.md instead.
/.github/skills/
migration-log.md
# Worktree-only convenience symlinks to the main checkout's data/ dir and
# one-shot operator scripts (filenames prefixed `_`) stay outside the
# committed project surface.
data
apps/runner/scripts/_*.mjs

# Personal operator-profile seed (local voice data; never committed).
# See apps/runner/operator-profile.seed.example.json for the shape.
/apps/runner/operator-profile.seed.json

# PM bridge runtime log (contains conversation content) - never commit
pm_browser_log.jsonl
/.mobile-audit/
